Task 1 secret rotation evidence - 2026-05-18

Implemented routine:
- private.rotate_integration_config_secret(p_key_name text, p_secret_value text, p_actor_id uuid, p_reason text, p_source text, p_request_id text)
- public.rotate_integration_config_secret(...) wrapper delegates to the private routine and is granted only to service_role.

Rotation behavior covered by SQL-shape test:
- Requires auth.role() = service_role.
- Rejects unknown config keys and non-secret keys.
- Uses pg_advisory_xact_lock on the key name for atomic per-key version allocation.
- Creates a new Vault secret with vault.create_secret using the supplied value.
- Stores only vault_secret_id, masked_value, value_fingerprint, version metadata, current pointer, and safe audit metadata in private tables.
- Does not return runtime plaintext from rotation; return columns are key_name, version_id, version_number, masked_value, value_fingerprint, updated_at.

Placeholder safety:
- No real provider secrets were used.
- Test fixtures do not include test_midtrans_key_placeholder, test_biteship_key_placeholder, or test_expo_token_placeholder.
- No raw provider key, bearer token, or decrypted secret value is present in evidence.

Verification commands:
- pnpm test supabase/migrations/__tests__/database-backed-integration-config-migration.test.ts: PASS, 9 tests.
- pnpm test: PASS, 670 tests.
- pnpm build: PASS.
